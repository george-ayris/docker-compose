worker_processes 4;

events { worker_connections 1024; }

http {

	upstream get_rack {
	      server getnode1:8080;
	      server getnode2:8080;
	}

	upstream post_rack {
	      server postnode1:8080;
	}
	 
	server {
	      listen 80;
	 
	      location / {
	        if ($request_method = POST)
		    {
		        proxy_pass http://post_rack;
		        break;
		    }
		 
		    proxy_pass http://get_rack;
	      }
	}
}